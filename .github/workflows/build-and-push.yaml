# // Copyright (c) 2i2c-org
# // Distributed under the terms of the Modified BSD License.

name: Build and push container image

on:
  push:
    branches:
      - main
    paths:
      - "binder/*"
  pull_request:
    branches:
      - main
    paths:
      - "binder/*"

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - name: Set Environment Variables
        run: |

          echo "DATE_TAG=$(TZ='America/Seattle' date +'%Y.%m.%d')" >> "$GITHUB_ENV"

      - name: Checkout files in repo
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 # main

      - name: Build and push the image to quay.io ${{ matrix.environment }}
        uses: jupyterhub/repo2docker-action@92fefcbd2b46dd9716f5263a9e740cbc972f6dd9 # master
        with:
          # Make sure username & password/token pair matches your registry credentials
          DOCKER_USERNAME: ${{ secrets.QUAY_USERNAME }}
          DOCKER_PASSWORD: ${{ secrets.QUAY_PASSWORD }}
          DOCKER_REGISTRY: "quay.io"
          ADDITIONAL_TAG: ${{ env.DATE_TAG }}

          # Disable pushing a 'latest' tag, as this often just causes confusion
          # LATEST_TAG_OFF: true
          #
          IMAGE_NAME: "developmentseed/warp-resample-profiling"
